# AI 使用紀錄 - Week 01

1. 問了哪些問題：
   - 如何在 Python 中優雅地整合多項基礎語法？
   - 如何使用推導式配合類別屬性進行資料過濾？
2. AI 給了哪些建議我有採用：
   - 使用 `@property` 裝飾器來封裝計算邏輯（如總庫存計算）。
   - 採用 `f-string` 的對齊格式碼來美化 CLI 輸出。
3. AI 有哪些建議我拒絕：
   - 建議使用 `dataclasses`，但為了展示基礎類別定義（Topic 10），決定手寫 `__init__`。
4. AI 可能誤導：
   - 在 lambda 排序建議中漏掉了 `reverse=True` 的參數需求，經查閱文檔後自行補上。
